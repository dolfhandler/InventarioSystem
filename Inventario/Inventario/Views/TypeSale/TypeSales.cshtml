@using Inventario.Models;
@using System.Web;
@model List<TypeSaleModel>
@{
    var title = "Tipos de venta";
    var types = Model != null ? Model : new List<TypeSaleModel>();
    var messageType = ViewBag.messageType != null ? ViewBag.messageType : "success";
    var message = ViewBag.message != null ? ViewBag.message : "";
}

<h1>
    @title
    <a href="/TypeSale/Create" class="btn btn-primary float-right">Nuevo</a>
</h1>

@if (!String.IsNullOrEmpty(message)) {
    <div class="alert alert-@messageType alert-dismissible fade show message" role="alert">
        @message
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}

@if (types.Count > 0) {
    <div class="table table-responsive">
        <table id="inventoryTable" class="display compact wrap hover">
            <thead>
                <tr>
                    <th>Descripción</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var type in Model) {
                    <tr>
                        <td>@HttpUtility.HtmlDecode(type.description)</td>
                        <td>
                            <a href="/TypeSale/Update/@type.id">Editar</a>
                            <a href="/TypeSale/Delete/@type.id">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
} else {
    <h5 class="text-warning">No existen tipos de ventas</h5>
    <a href="/TypeSale/Create">Crear tipo de venta</a>
}

@section Scripts{

}
